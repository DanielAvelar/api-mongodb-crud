extends ./layout

block content
	h1.
		#{title} Success.
	p #{message}
	form(action='/api/getBooks',method='get',name='createUser',enctype='application/x-www-form-urlencoded')
			button#btnSubmit(type="submit") continue